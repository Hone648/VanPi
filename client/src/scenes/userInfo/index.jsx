
const UserInfo = ({ data }, error, isLoading) => {
    (isLoading) && <span>Loading...</span>

    console.log(data)
    return (
        <>
            <div>sdsa</div>
        </>
    )
}

export default UserInfo;